    /* esy functions jinko use krke hum different calculations use kr skty table aur usky results pe*/

SELECT COUNT (dept) FROM faculty;/* counting total rows*/

SELECT SUM(salary) FROM faculty; /*adding total salaries*/

SELECT AVG(salary) FROM faculty;/* finding average salary*/

SELECT MIN(salary) FROM faculty; /*minimum salary*/

SELECT MAX(salary) FROM faculty; /*maximum salarty*/